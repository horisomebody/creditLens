# CreditLens

설명 가능한 개인 대출 연체 위험 예측 시스템. 금융권(카드사·증권사) 취업 포트폴리오용 1년 캡스톤 프로젝트.
상세 계획은 `docs/project-plan.md`를 참고한다.

이 문서의 항목은 **확정 / 제안(미확정)**으로 구분된다. 확정 항목은 임의로 바꾸지 않는다.
제안 항목은 구현 전에 반드시 사용자에게 선택지와 트레이드오프를 제시하고 확인받는다.

## 확정된 결정

- **데이터**: Kaggle Give Me Some Credit(GMC, 비교군) + Home Credit Default Risk(주 데이터셋).
  원래는 GMC 단독이었으나 2026-09-17 사용자 승인으로 변경했다.
  **변경 사유**: GMC 스파이크 결과, 로지스틱 회귀(홀드아웃 AUC 0.8598)와 튜닝된 XGBoost(AUC 0.8691)의 차이가
  +0.0094에 그쳤다. 상위 5% 고위험군 포착 성능도 정밀도 47.47% vs 48.00%, 포착률 35.51% vs 35.91%,
  lift 7.10배 vs 7.18배로 사실상 동률이었고, 피처 엔지니어링을 추가하자 격차가 오히려 좁혀졌다
  (+0.0093 → +0.0074, 상세는 `docs/spike-feasibility.md`). 즉 변수 10개짜리 단순 정형 데이터에서는
  비선형 모델의 성능 이점이 사실상 없었고, 이는 "설명이 어려워지는 비용을 감수하고 XGBoost를 쓸 이유"와
  SHAP 기반 설명 체계라는 프로젝트 핵심 축의 근거를 약화시켰다. GMC는 버리지 않고 비교군으로 유지하며,
  다중 테이블·고결측·범주형 구조를 가진 Home Credit Default Risk를 주 데이터셋으로 추가해 데이터 복잡도에
  따른 모델 성능 격차를 실증적으로 비교한다.
- **연구 질문**: 데이터의 복잡도(변수 수, 테이블 구조)에 따라 선형 모델과 비선형 모델의 성능 격차가 어떻게
  달라지는가? 그리고 비선형 모델을 선택해야 하는 조건에서, SHAP 기반 설명 체계가 스코어카드에 준하는
  설명력을 확보할 수 있는가?
- **모델 선택 방식**: 데이터셋(GMC, Home Credit)마다 로지스틱 회귀 베이스라인과 튜닝된 XGBoost를 비교해
  해당 트랙의 최종 모델을 정한다. 특정 모델을 미리 확정하지 않는다. GMC 쪽 스파이크 비교는 완료(근소한 XGBoost
  우위, 위 변경 사유 참고) — 정식 비교는 `docs/project-plan.md` Phase 3에서, Home Credit 비교는 Phase 6에서 진행.
- **설명**: SHAP 기여도 기반이 기본 방향. 단, 데이터셋별로 로지스틱 회귀가 최종 채택되면 계수·WoE 기반 설명으로
  전환한다. Home Credit에서 비선형 모델이 채택될 경우, SHAP 설명이 스코어카드 수준의 설명력을 확보하는지 자체가
  검증 대상이다(Phase 7).
- **사유 코드**: 템플릿 기반. LLM으로 설명문을 생성하지 않는다(단순 설명에는 템플릿으로 충분하다고 판단해 범위 제외).
- **운영**: Spring Batch 일일 재스코어링 파이프라인. 대상 데이터셋(GMC vs Home Credit)은 미결정 —
  `docs/project-plan.md` 미결정 사항 참고, Phase 8 비교 분석 이후 결정.
- **기간**: 1년

## 제안 (미확정 — 구현 전 확인 필요)

아래는 논의 중 나온 제안이며 아직 채택되지 않았다.

- 로지스틱 회귀 + WoE 스코어카드를 기준 모델로 두고 XGBoost와 비교
  → 로지스틱 회귀를 베이스라인으로 비교한다는 원칙 자체는 위 "모델 선택 방식"으로 확정됨. 정식 WoE 구간화·
    스코어카드 구현 여부(특히 Home Credit)는 여전히 미확정.
- XGBoost 출력 확률 보정(Platt / Isotonic) 후 등급 매핑
- PSI 기반 드리프트 모니터링과 대응 흐름
- 모델 버전 관리, 챔피언–챌린저 병행 평가
- Vue 대시보드
- 추천 알고리즘은 범위에서 제외

## 데이터 주의사항

### Give Me Some Credit — 비교군 (스파이크로 실측 검증 완료, 2026-09-17)

- 타깃 `SeriousDlqin2yrs`: 향후 2년 내 90일 이상 연체. 양성 비율 6.68%(약 7%)로 불균형.
- `cs-test.csv`에는 `SeriousDlqin2yrs` 컬럼은 있으나 값이 전부 결측(대회 채점용, 비공개).
  **성능 평가는 `cs-training.csv`를 분할해서 한다.**
- `MonthlyIncome` 결측 19.82%, `NumberOfDependents` 결측 2.62%.
- 소득 결측·0인 행에서 `DebtRatio` 중앙값이 1,144(정상 소득 구간 0.292 대비 약 3,900배)로 비정상적으로 큼.
- 연체 횟수 3개 변수에 96, 98 코드값 269건 존재. 세 변수는 서로 강하게 상관되며, SHAP 상위 기여 변수도
  이 세 변수가 독점(스파이크의 개별 고객 사례 확인).
- `RevolvingUtilizationOfUnsecuredLines` 1 초과 3,321건(2.21%) 존재.
- `age` ≤ 0 행 1건 존재. `age`는 민감 변수라 모델 사용 및 사유 코드 노출 정책을 정해야 함.
- 날짜 정보 없음 → 시간 순 분할·실제 드리프트 관측 불가.
- 첫 번째 컬럼은 이름 없는 인덱스.
- Kaggle 대회 규칙상 재배포 제한 가능성 → **원천 CSV를 저장소에 커밋하지 않는다.**
- 상세 수치와 스파이크 결과: `docs/spike-feasibility.md`.

### Home Credit Default Risk — 주 데이터셋 (다운로드 완료, 2026-09-17. 아래는 아직 공개 자료 기준이며 Phase 5 EDA에서 실측 검증 필요)

- 다중 테이블 구조: `application_train/test` 외 `bureau`, `bureau_balance`, `previous_application`,
  `POS_CASH_balance`, `credit_card_balance`, `installments_payments` 등을 조인해야 전체 피처를 구성할 수 있다.
- 결측률이 높은 컬럼이 다수 존재(공개 자료 기준). GMC보다 훨씬 세밀한 컬럼별 결측 처리 방침이 필요하다.
- 범주형 변수 다수 존재 — GMC는 전부 수치형이었다. 인코딩 전략(원-핫, 타깃 인코딩 등)을 새로 정해야 한다.
- `EXT_SOURCE_1/2/3` 등 정의가 공개되지 않은 블랙박스성 신용 점수 변수가 있다 — SHAP 기여도는 보여줄 수 있어도
  근본 설명은 어려움. 이 한계 자체가 연구 질문 검증 대상이다.
- 데이터 용량이 GMC보다 훨씬 커서(약 2.5GB) 원천 파일은 저장소에 커밋하지 않는다. `data/raw_home_credit/`에 보관(2026-09-17 다운로드 완료).
- 과거 이 프로젝트에서 "블랙박스 변수, 비공개 타깃 정의, 민감 변수 다수로 설명 가능성 불리"라는 이유로
  채택하지 않았던 이력이 있다. 이번 재정의로 그 우려 자체가 검증 대상이 됨
  (`docs/project-plan.md` "검토 후 채택하지 않은 대안" 항목 참고).

## 기술 스택

- ML: Python, pandas, scikit-learn, xgboost, shap
- 백엔드: Java / Spring Boot, Spring Batch, MyBatis
- 프론트엔드: Vue.js
- 인프라: Redis, Docker Compose, nginx, AWS EC2, GitHub Actions CI/CD

## 작업 방식

- `docs/project-plan.md`의 Phase 순서대로 진행한다. 한 번에 전체를 만들지 않는다.
- 각 Phase 시작 전 산출물과 완료 기준을 확인한다.
- 미결정 사항과 제안 항목은 임의로 정하지 않는다.
- Phase가 끝나면 아래 진행 상황을 갱신한다.
- 커밋 메시지, 주석, 문서는 한국어로 작성한다.

## 진행 상황

- [ ] Phase 0. 프로젝트 골격 및 환경 구성
- [ ] Phase 1. GMC 데이터 탐색·품질 점검 (스파이크로 사전 검증 완료, 정식 산출물은 미작성)
- [ ] Phase 2. GMC 전처리·피처
- [ ] Phase 3. GMC 모델 비교 (로지스틱 회귀 vs XGBoost)
- [ ] Phase 4. GMC 설명 체계 (SHAP 또는 스코어카드 사유 코드)
- [ ] Phase 5. Home Credit 데이터 탐색·전처리·피처 (다중 테이블)
- [ ] Phase 6. Home Credit 모델 비교 (로지스틱 회귀 + WoE 검토 vs XGBoost)
- [ ] Phase 7. Home Credit 설명 체계 및 설명력 검증
- [ ] Phase 8. 두 데이터셋 비교 분석 및 최종 모델·설명 방식 결정
- [ ] Phase 9. 스코어링·Spring Batch 파이프라인
- [ ] Phase 10. 배포·문서화
- [ ] 선택 확장 (제안 항목 중 채택된 것)
